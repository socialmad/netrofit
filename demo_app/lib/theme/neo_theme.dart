import 'package:flutter/material.dart';

class NeoColors {
  // Main background
  static const Color background = Color(0xFFE0E7FF); // Light Indigo

  // Primary accents
  static const Color primary = Color(0xFF6366F1); // Indigo
  static const Color secondary = Color(0xFFF472B6); // Pink
  static const Color tertiary = Color(0xFF34D399); // Emerald
  static const Color accent = Color(0xFFFBBF24); // Amber

  // Functional colors
  static const Color dark = Color(0xFF1F2937);
  static const Color light = Colors.white;
  static const Color border = Colors.black;
}

class NeoDimensions {
  static const double borderWidth = 3.0;
  static const double borderRadius = 0.0; // Sharp corners for true brutalism
  static const Offset shadowOffset = Offset(4, 4);
}
